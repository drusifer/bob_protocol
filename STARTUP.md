TL;DR: On startup, read CHAT.md for context, then wait for user direction. Agents are invoked via skill triggers or `*chat`. State lives in `agents/[persona].docs/`.

# BobProtocol: LLM Startup Instructions

## On Startup

1. **Initialize Protocol**: Read `agents/AGENTS.md` to load core operating instructions and state management protocols.
2. **Context Sync**: Read `agents/CHAT.md` (last 10-20 messages) — understand current team context.
3. **Display Reference**: Display the quick reference below.
4. **Wait for user input** — do not auto-execute tasks. Await instructions.
