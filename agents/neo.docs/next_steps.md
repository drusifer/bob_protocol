# Next Steps

## Immediate Next Action
Restart Codex so the MCP client starts with the updated `via` registration.

## Waiting On
User/Codex restart to confirm startup banner no longer reports `via` handshake failure.

## Planned Work
- [ ] If startup still fails after restart, inspect `.via/mcp.log` and run a full MCP client initialize probe outside the sandbox.

---
*Last updated: 2026-04-12T12:56*
